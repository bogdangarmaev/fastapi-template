# Шаблон бэкэнда FastAPI

Использование pre-commit:

```shell
pre-commit install
pre-commit install --hook-type prepare-commit-msg
```

Запуск проекта:
```shell
make run_detached
```
Запуск тестов:
```shell
make test
```
